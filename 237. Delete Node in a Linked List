 void deleteNode(ListNode* node) {
        
        ListNode *q=node->next;
        
        node->val=q->val;
        node->next=q->next;
        delete q;
        q=node->next; 

              
        
    }
